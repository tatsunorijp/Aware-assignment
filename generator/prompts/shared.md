# Shared client generation context

This file routes context; it is not a generation request. Work from the repository
root and follow the pasted prompt.

Read the root and selected platform AGENTS files plus the platform README. Inspect
the affected code/tests, identify the task domains, and use the root documentation
map to open only their canonical owners and relevant direct references. For UI,
start with the UX file named by the task and inspect its actual prototypes.

If a required product decision is still absent, follow the repository ambiguity
workflow: identify its owner, ask the developer and wait. Do not widen scope or
change the server/shared contract to bypass missing context or foundation.

Stay inside the generator boundary and reuse maintained platform foundations.
Generate only authorized feature code/tests, run relevant native checks, and
report actual results and unavailable checks. When an acceptance scenario crosses
transport, service, ViewModel and UI boundaries, do not treat direct injection of
its final presentation state as proof that the boundary transition works. Keep
repository artifacts and UI copy in English.
