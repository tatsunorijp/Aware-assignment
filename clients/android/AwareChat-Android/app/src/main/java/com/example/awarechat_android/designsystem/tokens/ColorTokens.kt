package com.example.awarechat_android.designsystem.tokens

import androidx.compose.ui.graphics.Color

object ColorTokens {
    /** Outgoing messages, primary actions, and server-acceptance indicators. */
    val primary = Color(0xFF0191C4)

    /** Incoming messages and secondary actions. */
    val secondary = Color(0xFFF0F4FA)

    /** Main screen and full-screen component background. */
    val background = Color(0xFFFFFFFF)

    /** Names, titles, and text on light surfaces. */
    val textPrimary = Color(0xFF07143F)

    /** Dates, times, and placeholder text. */
    val textSecondary = Color(0xFF8796B8)

    /** Subtle separators and borders. */
    val divider = Color(0xFFE6EAF1)

    /** Failed-message indicators and destructive error accents. */
    val customRed = Color(0xFFFF0000)
}
